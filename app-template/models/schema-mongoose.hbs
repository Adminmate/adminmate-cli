// This model was generated by the adminmate-cli. You are free to adapt it ;)

module.exports = (mongoose) => {
  const schema = mongoose.Schema({{{json jsonSchema}}});

  return mongoose.model('{{modelName}}', schema, '{{modelName}}');
};